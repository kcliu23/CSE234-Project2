# Inference dependencies for main.py.
# TAs running `python3 main.py --input ... --output ...` only need these.
# Pinned to the exact versions used to produce predictions/preds_4way_maj2.json
# on DSMLP (24 GB MIG slice, CUDA 12.x). Newer minor versions should also work.

torch==2.10.0
transformers==4.57.6
peft==0.19.1
sentence-transformers==5.1.0
safetensors==0.7.0
accelerate==1.13.0
numpy==2.2.6
gdown==6.1.0           # only used by main.py:_ensure_adapters() fallback if a
                       # LoRA adapter folder is missing locally (e.g. partial clone)

# --- Training-time only (NOT required for inference / grading) ---
# Uncomment to reproduce training (training/train_rapidfire.py, data_prep/):
# trl==1.4.0
# rapidfireai==0.15.2
# datasets
# sqlglot==30.8.0          # used by data_prep/sql_to_schema_links.py
# bitsandbytes==0.49.2     # optional, only if you switch to QLoRA
